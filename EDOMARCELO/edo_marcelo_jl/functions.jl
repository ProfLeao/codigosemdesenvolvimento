𝑘(θ) = if θ >= 20 && θ <= 800
	54 - 3.33e-2 * θ
elseif θ > 800 && θ <= 1515.
	27.3
end

cv(θ) = if θ >= 20. && θ <= 600.
	425. + 7.73e-1 * θ - 1.69e-3 * θ^2 + 2.22e-6 * θ^3
elseif θ > 600. && θ <= 735.
	666. + 13002. / (738. - θ)
elseif θ > 735. && θ <= 900.
	525. + 17820. / (θ - 731.)
elseif θ > 900. && θ <= 1515.
	650.
end

ρ(θ) = if θ >= 25. && θ <= 689.
	-0.3373 * θ + 7871.
elseif θ > 689. && θ <= 853.
	0.1226 * θ + 7556.
elseif θ > 853. && θ <= 1515.
	-0.5575 * θ + 8158.
end

𝒉(𝒛) = if 𝒛 >= 0. && 𝒛 <= 9.
	1175.
elseif 𝒛 > 9. && 𝒛 <= 15.
	115.
end